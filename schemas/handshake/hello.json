{
  "$defs": {
    "Capability": {
      "description": "A named, optional feature of one peer.\n\nCapabilities are compared by string equality and are never parsed. A peer\nthat does not recognise one must ignore it.",
      "type": "string"
    },
    "PeerInfo": {
      "description": "Who is on the other end. Diagnostic only; never used for authorization —\n§4.3 is explicit that authorization is filesystem/pipe permissions.",
      "properties": {
        "build": {
          "description": "Human-readable build version, e.g. `\"0.1.0\"`. Not the protocol version.",
          "type": "string"
        },
        "name": {
          "description": "e.g. `\"shepctl\"`, `\"shepherd-ui\"`, `\"shepherdd\"`.",
          "type": "string"
        }
      },
      "required": [
        "name",
        "build"
      ],
      "type": "object"
    },
    "ProtoVersion": {
      "description": "A `major.minor` protocol version.\n\nSerialized as an object rather than a `\"1.0\"` string so a future `patch` or\nbuild-metadata field is an additive change instead of a parse change.",
      "properties": {
        "major": {
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "minor": {
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "major",
        "minor"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "The first frame a client sends on a new connection.",
  "properties": {
    "capabilities": {
      "default": [],
      "description": "Capabilities the client can make use of. Empty is legal.",
      "items": {
        "$ref": "#/$defs/Capability"
      },
      "type": "array"
    },
    "client": {
      "$ref": "#/$defs/PeerInfo"
    },
    "proto_version": {
      "$ref": "#/$defs/ProtoVersion"
    }
  },
  "required": [
    "proto_version",
    "client"
  ],
  "title": "hello (connection preamble)",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.2",
    "registry_fingerprint": "ec2b4050376aac73",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
