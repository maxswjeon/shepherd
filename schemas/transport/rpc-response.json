{
  "$defs": {
    "JsonRpcV2": {
      "description": "The `\"2.0\"` literal, as a type.\n\nA `String` field would let a peer send `\"1.0\"` and have it parse. This\nrejects anything else at deserialization, which is where\n[`crate::ErrorCode::InvalidRequest`] belongs.",
      "type": "null"
    },
    "RequestId": {
      "anyOf": [
        {
          "format": "int64",
          "type": "integer"
        },
        {
          "type": "string"
        }
      ],
      "description": "A JSON-RPC request id. The spec permits a string or a number; both are\naccepted so a client may use whichever its language makes natural."
    },
    "RpcError": {
      "description": "A JSON-RPC 2.0 error object.",
      "properties": {
        "code": {
          "description": "The JSON-RPC numeric code. Interpret via [`RpcError::kind`]; an\nunrecognised value is a forward-compatibility case, not a fault.",
          "format": "int32",
          "type": "integer"
        },
        "data": {
          "description": "Structured detail. Free-form by design: it is diagnostic, not a contract,\nand nothing may branch on its shape."
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "One response frame. Newline-delimited on the socket (§4.3).\n\nExactly one of `result` and `error` is present, per JSON-RPC 2.0. That is a\nspec invariant rather than a type invariant here, because a peer may violate\nit and the parse must survive long enough to report which peer did.\n[`RpcResponse::outcome`] is the checked accessor.",
  "properties": {
    "error": {
      "anyOf": [
        {
          "$ref": "#/$defs/RpcError"
        },
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "anyOf": [
        {
          "$ref": "#/$defs/RequestId"
        },
        {
          "type": "null"
        }
      ],
      "description": "`None` only when the request could not be parsed well enough to recover\nits id, which JSON-RPC 2.0 renders as `null`."
    },
    "jsonrpc": {
      "$ref": "#/$defs/JsonRpcV2"
    },
    "result": true
  },
  "required": [
    "jsonrpc"
  ],
  "title": "JSON-RPC response frame",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.3",
    "registry_fingerprint": "ec2b4050376aac73",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
