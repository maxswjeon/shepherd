{
  "$defs": {
    "ExcludedCandidate": {
      "properties": {
        "file_id": {
          "format": "int64",
          "type": "integer"
        },
        "reason": {
          "description": "The floor or policy that excluded it.",
          "type": "string"
        },
        "rel_path": {
          "type": "string"
        }
      },
      "required": [
        "file_id",
        "rel_path",
        "reason"
      ],
      "type": "object"
    },
    "FileState": {
      "description": "A file's local materialization state, as the catalog records it.",
      "enum": [
        "local",
        "stub",
        "remote",
        "missing"
      ],
      "type": "string"
    },
    "SearchHit": {
      "description": "One search hit.",
      "properties": {
        "blake3": {
          "default": null,
          "description": "Lowercase hex BLAKE3, or `None` while the file is still unhashed —\nhashing is its own job class and never gates cataloguing (§6 Phase 1).",
          "type": [
            "string",
            "null"
          ]
        },
        "file_id": {
          "format": "int64",
          "type": "integer"
        },
        "mtime": {
          "format": "int64",
          "type": "integer"
        },
        "rel_path": {
          "type": "string"
        },
        "root_id": {
          "format": "int64",
          "type": "integer"
        },
        "score": {
          "default": null,
          "description": "Relevance, higher is better. Absent for a pure metadata query, where\nordering is by the requested sort and a score would be invented.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "size": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "state": {
          "$ref": "#/$defs/FileState"
        },
        "tags": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "file_id",
        "root_id",
        "rel_path",
        "size",
        "mtime",
        "state"
      ],
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`tier.plan`.",
  "properties": {
    "bytes_total": {
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "candidate_count": {
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "candidate_set_hash": {
      "description": "Lowercase hex BLAKE3 over the candidate set. `tier.run` must echo it\nback; §4.10's bulk breaker confirms against the set the user saw.",
      "type": "string"
    },
    "excluded": {
      "default": [],
      "description": "Candidates excluded by a safety floor, with the floor named. Present\neven when empty: \"nothing was excluded\" and \"exclusions were not computed\"\nmust not look alike.",
      "items": {
        "$ref": "#/$defs/ExcludedCandidate"
      },
      "type": "array"
    },
    "plan_id": {
      "type": "string"
    },
    "rule_id": {
      "format": "int64",
      "type": "integer"
    },
    "sample": {
      "items": {
        "$ref": "#/$defs/SearchHit"
      },
      "type": "array"
    },
    "target_id": {
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "plan_id",
    "rule_id",
    "target_id",
    "candidate_count",
    "bytes_total",
    "candidate_set_hash",
    "sample"
  ],
  "title": "tier.plan result",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.1",
    "registry_fingerprint": "ec2b4050376aac73",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
