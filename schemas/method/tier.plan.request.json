{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`tier.plan` — compute a candidate set. Read-only; moves nothing.",
  "properties": {
    "limit": {
      "default": null,
      "description": "Cap the candidate set. The returned counts describe the capped set.",
      "format": "uint32",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "rule_id": {
      "description": "The rule that selects candidates.",
      "format": "int64",
      "type": "integer"
    },
    "target_id": {
      "description": "Where the candidates would be sent.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "rule_id",
    "target_id"
  ],
  "title": "tier.plan request",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.0",
    "registry_fingerprint": "0a9109ca86bf2c18",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
