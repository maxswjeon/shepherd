{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`restore` — bring bytes back from a target.\n\nExactly one of `file_id` or `path` must be set; the daemon answers\n[`crate::ErrorCode::Invalid`] otherwise. Expressing \"exactly one of\" in JSON\nSchema is possible but unreadable, and a `oneOf` in the committed artifact\nwould be harder to review than the daemon-side check it replaces.",
  "properties": {
    "destination": {
      "default": null,
      "description": "Where to write. `None` restores in place, which is the AC-5 fidelity\ncase: bytes, mtime and mode must all match the original.",
      "type": [
        "string",
        "null"
      ]
    },
    "file_id": {
      "default": null,
      "description": "The catalog id of the file to restore. Mutually exclusive with `path`.",
      "format": "int64",
      "type": [
        "integer",
        "null"
      ]
    },
    "overwrite": {
      "default": false,
      "description": "Without this, restore uses an exclusive create and fails if anything is\nalready at the destination (§4.10).",
      "type": "boolean"
    },
    "path": {
      "default": null,
      "description": "The path of the file to restore. Mutually exclusive with `file_id`.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "title": "restore request",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.3",
    "registry_fingerprint": "ec2b4050376aac73",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
