{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`rule.preview` — the mandatory dry run (AC-14).",
  "properties": {
    "limit": {
      "default": null,
      "description": "How many matched rows to return as a sample. The counts are exact\nregardless.",
      "format": "uint32",
      "minimum": 0,
      "type": [
        "integer",
        "null"
      ]
    },
    "rule_id": {
      "description": "The rule to dry-run.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "rule_id"
  ],
  "title": "rule.preview request",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.2",
    "registry_fingerprint": "ec2b4050376aac73",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
