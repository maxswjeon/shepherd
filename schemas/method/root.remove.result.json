{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`root.remove`.",
  "properties": {
    "catalog_rows_dropped": {
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "custody_rows_dropped": {
      "description": "How many dropped rows were `Custody` class — the only address of bytes\nthat no longer exist locally. Reported rather than merely counted,\nbecause it is the number that makes a `--force` regrettable.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "root_id": {
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "root_id",
    "catalog_rows_dropped",
    "custody_rows_dropped"
  ],
  "title": "root.remove result",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.2",
    "registry_fingerprint": "ec2b4050376aac73",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
