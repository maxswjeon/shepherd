{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "`root.remove` — deregister a scan root.\n\nNever touches file bytes. `forget_catalog` drops the *rows*, and dropping\nrows for a root that holds `Custody`-class files destroys the only address of\ndata that no longer exists locally — so the daemon refuses with\n[`crate::ErrorCode::Refused`] unless `force` is set.",
  "properties": {
    "force": {
      "default": false,
      "description": "Proceed even when dropping rows would discard `Custody`-class records —\nthe only address of bytes that no longer exist locally.",
      "type": "boolean"
    },
    "forget_catalog": {
      "default": false,
      "description": "Also drop the root's catalog rows. File bytes are never touched.",
      "type": "boolean"
    },
    "root_id": {
      "description": "The root to deregister.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "root_id"
  ],
  "title": "root.remove request",
  "type": "object",
  "x-shepherd": {
    "generator": "cargo xtask codegen",
    "generator_version": 1,
    "proto_version": "1.0",
    "registry_fingerprint": "0a9109ca86bf2c18",
    "warning": "GENERATED FILE — edit crates/shepherd-proto and re-run `cargo xtask codegen`."
  }
}
