[package]
name = "shepherd-proto"
description = "IPC wire types and the method table: the single source of truth for the JSON-RPC surface."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
