//! Durable job queue, workers, cron scheduling, catch-up and adaptive concurrency.
//!
//! Phase 0a skeleton: this crate exists so the workspace dependency graph is
//! complete and `cargo xtask check-deps` can enforce §4.1's rules before the
//! code they constrain is written.
