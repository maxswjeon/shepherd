//! Placeholder — filled in after the dependency smoke test passes.
use crate::Args;
pub fn gen_trace(_a: &Args) -> Result<(), String> {
    Err("not yet".into())
}
pub fn gen_catalog(_a: &Args) -> Result<(), String> {
    Err("not yet".into())
}
