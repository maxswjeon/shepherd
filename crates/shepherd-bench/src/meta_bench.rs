//! Placeholder — filled in after the dependency smoke test passes.
use crate::Args;
pub fn build(_a: &Args) -> Result<(), String> {
    Err("not yet".into())
}
pub fn bench(_a: &Args) -> Result<(), String> {
    Err("not yet".into())
}
